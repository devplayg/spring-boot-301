package com.devplayg.hippo.framework

import com.devplayg.hippo.entity.Member
import mu.KLogging
import org.springframework.context.annotation.Configuration


//@Component
//@Configuration
//class InMemoryMemberManager :  {
//    companion object : KLogging()
//
//    init {
////        Members.selectAll().map {
////            memberRedisRepository.save(it)
////        }
//
//        logger.info("################################### --- InMemoryMemberManager()")
//        Member.all().map {
////            memberRedisRepository.save(it)
//        }
//    }
////    fun init() {
////        var members: List<MemberDto>
////        transaction {
////            Member.all().map {
////                memberRedisRepository.save(it)
////            }
////            Members.selectAll().map {
////
////                memberRedisRepository.save(it)
////            }
////            memberRedisRepository.sa
//}
